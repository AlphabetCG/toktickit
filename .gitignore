# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# prisma
server/prisma/*.db
# uploaded attachments (Lab 2) — metadata lives in the database
server/uploads/
# playwright
test-results/
playwright-report/
blob-report/
.playwright/
# root lockfile — the E2E root package.json is committed, but per-package
# lockfiles live in client/ and server/ only (AGENTS.md)
/package-lock.json
# claude skills lockfile (local tooling, not part of the lab)
skills-lock.json
# logs & OS
*.log
.DS_Store
# editor
.vscode/
.idea/
Thumbs.db
# test coverage
coverage/
