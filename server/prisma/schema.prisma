// Prisma schema for TokTickIT.
// Issue 1 initializes Prisma with a PostgreSQL datasource and client generator.
// The Category model and migration are added in Issue 3.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
